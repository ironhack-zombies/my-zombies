<div class="catalog">
{{#each userId.zombiesOwned}}
      <div class="ownedZombie shopZombie">
        <div class="imageWrap">
            <img src="{{origin.image}}">
        </div>
        <h3>{{origin.type}}</h3>
        <ul>
        {{#each origin.description}}
          <li>{{this}}</li>
        {{/each}}
        </ul>
      </div>
{{/each}}
</div>