<div class="arena">
  <h1>Welcome to the zombie arena</h1>
  {{#if zombies}}
  <p>Select one of your zombies to fight</p>
  <form class="arena" action="/village/arena/fight" method="POST">
    <div class="selectBox">
    <select name="zombie" id="zombie">
      {{#each zombies}}
      <option value="{{_id}}">{{nickname}}</option>
      {{/each}}
    </select>
    </div>
    <button class="backButn" type="submit">Fight! (Cost: 2 <i class="fas fa-brain"></i>)</button>
  </form>
  {{else}}
  <p>You currently have no zombies that are ready to fight</p>
  {{/if}}
  {{#if message}}
  <p>{{message}}</p>
  {{/if}}
</div>