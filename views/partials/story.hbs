<h1>{{story.title}}</h1>
<p>{{story.text}}</p>
<form action="/story/{{story._id}}/like" method="POST">
<input type="submit" value="{{#ifIn user.id story.likes }}Liked{{else}}Like{{/ifIn}}" {{#ifIn user.id story.likes }}disabled{{/ifIn}}>
</form>